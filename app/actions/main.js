/* @flow */

/**
 * Redux actions for cards
 * @module actions/cards
 */

import { createAction } from 'redux-actions'

/**
 * COMPLEX ACTIONS [redux thunk middleware].
 */


/**
 * SIMPLE ACTIONS [redux default middleware].
 */
